package num;

/**
 * 
 * @author Guest
 */
interface Constants {
    /**
     * 
     */
    double E = Math.E;
    
    /**
     * The ratio of a circle's circumference to its diameter.
     */
    double PI = Math.PI;
    
    
    /**
     * The square root of 2.
     */
    double RT2 = Math.sqrt(2);
    
    /**
     * The golden ratio, such that (a + b) / a and a / b are equal for a > b.
     */
    double PHI = (1 + Math.sqrt(5)) / 2;
    
    long MAX_THEORETICAL_ARMSTRONG_NUMBER = 9 * Tabler.XPOWY[9][9];
}
